# ----------------------
# Python
# ----------------------
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

dist/
build/
*.egg-info/

# Jupyter / notebooks
.ipynb_checkpoints/

# ----------------------
# Rust
# ----------------------
target/
# Cargo.lock is intentionally committed: this workspace only produces
# binaries/services (argusnetd, the viewer), not a published library, so the
# lockfile should stay reproducible across CI and local builds.

# ----------------------
# Generated outputs
# ----------------------
*.csv
*.log
*.tmp

# simulation / replay outputs
replay.json
replay_*.json
*_replay.json
# ...but test fixtures under tests/ are inputs, not outputs. Without this,
# `replay_*.json` silently swallowed tests/fixtures/replay_contract_fixture.json
# on `git add -A`, so the cross-language contract test passed locally and failed
# in CI with FileNotFoundError.
!tests/fixtures/**
*_large.json
*.frames.jsonl
*.frames.session.json
*.bak

# large datasets
*.dataset.json
*.sim.json

# terrain / generated scenes
scene_packages/
scenes/
data/
outputs/
replays/

# ----------------------
# Large binary assets
# ----------------------
*.glb

# ...except the bundled sample scene, which is intentionally tracked so the
# viewer has something to open out of the box. Without these negations the
# package only stays in the tree because it was force-added.
!scene.smartscene/**
*.gltf
*.bin
*.fbx
*.obj

# ----------------------
# OS / IDE
# ----------------------
.DS_Store
.playwright-cli/
.vscode/
.idea/